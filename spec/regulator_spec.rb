require 'spec_helper'

describe Regulator do
  it 'has a version number' do
    expect(Regulator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
